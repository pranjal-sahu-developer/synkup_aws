services:
  app:
    build:
      context: .
      args:
        VITE_API_URL: /api
        VITE_STREAM_API_KEY: ${VITE_STREAM_API_KEY}
    ports:
      - "5001:5001"
    env_file:
      - backend/.env
    environment:
      NODE_ENV: production
      PORT: 5001
    restart: unless-stopped
